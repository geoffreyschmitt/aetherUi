'use client';
import styled from 'styled-components';
import { ButtonList } from '@/aether-ui-core/components/lists/ButtonList';

export const RootElement = styled(ButtonList)`
  --button-list-gap: 4px;
`;
