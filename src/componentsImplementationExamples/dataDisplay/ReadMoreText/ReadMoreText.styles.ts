'use client';
import styled from 'styled-components';
import { ReadMoreText } from '@/components/dataDisplay/ReadMoreText';

export const RootElement = styled(ReadMoreText)`
  .read-more__button {
    margin-top: 8px;
  }
`;
